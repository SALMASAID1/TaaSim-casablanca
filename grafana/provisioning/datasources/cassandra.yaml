apiVersion: 1

datasources:
  - name: Cassandra
    uid: cassandra
    type: hadesarchitect-cassandra-datasource
    access: proxy
    url: cassandra:9042
    isDefault: true
    jsonData:
      keyspace: taasim
      consistency: LOCAL_ONE
